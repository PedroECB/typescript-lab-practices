/**
 *  tsc init - inicializar um projeto typescript com um arquivo tsconfig padrão
 *
 * noEmitOnError -> não gerar arquivo compilado js quando houver erro no arquivo .ts
 *
 * target -> Alterar as versões do JS que será gerada pelo arquivo .ts
 *
 * sourceMap -> Habilita a geração de um arquivo js. map com o código fonte equivalent
 * ao arquivo typescript original
 *
 * noImplicityAny -> possibilita a declaração automática do tipo any na criação das variáveis
 *
 * strictCheckNulls -> Verifica a existências de pontenciais variaveis nulas
 *
 * noUsedParams -> alertar a declaração de parâmetros não utilizados nas funções
 *
 * noUsedLocals -> Variáveis locais declaradas mas não utilizadas
 *
 *
 *
 *
 *
 *
 *  */
